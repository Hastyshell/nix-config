--- a/src/proxy/server.ts
+++ b/src/proxy/server.ts
@@ -465,7 +465,10 @@
           : undefined

       // Combine system context with conversation
-      const prompt = systemContext
+      // On resume the SDK session already contains the system prompt from the
+      // initial request, so skip it to avoid redundant token usage and improve
+      // prompt caching at the API level.
+      const prompt = (systemContext && !isResume)
         ? `${systemContext}\n\n${conversationParts}`
         : conversationParts

